# Runtime dependencies. A pinned snapshot lives in requirements.lock.txt.
# Install:  pip install -r requirements.txt

# --- Web ---
Flask>=3.0
Flask-Login>=0.6
Flask-WTF>=1.2
WTForms>=3.1
email-validator>=2.0     # WTForms Email() validator

# --- Persistence ---
SQLAlchemy>=2.0
Flask-SQLAlchemy>=3.1
Alembic>=1.13

# --- Scheduler ---
APScheduler>=3.10

# --- Crypto ---
cryptography>=42.0        # AES-256-GCM, SHA-256
argon2-cffi>=23.1         # Argon2id KDF / password hashing
quantcrypt>=1.0.1         # PQC backend: ML-DSA / SLH-DSA / ML-KEM (prebuilt PQClean)

# --- On-chain execution (docs/plans/onchain-execution.md) ---
# The relayer's Ethereum transactions are signed with secp256k1. That key only pays gas — the
# approvals themselves are ML-DSA-65 and checked by the contract — but it is still not something
# to hand-roll, so transaction signing and RLP come from the reference Python implementation.
eth-account>=0.14         # EIP-1559 transaction signing for the relayer
eth-abi>=6.0              # ABI encoding: must match Solidity's abi.encode byte for byte
pycryptodome>=3.23        # keccak-256 (eth-hash backend); hashlib's sha3_256 is NOT keccak

# --- Config ---
python-dotenv>=1.0
