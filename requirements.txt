# Runtime dependencies. A pinned snapshot lives in requirements.lock.txt.
# Install:  pip install -r requirements.txt

# --- Web ---
Flask>=3.0
Flask-Login>=0.6
Flask-WTF>=1.2
WTForms>=3.1

# --- Persistence ---
SQLAlchemy>=2.0
Flask-SQLAlchemy>=3.1
Alembic>=1.13

# --- Scheduler ---
APScheduler>=3.10

# --- Crypto ---
cryptography>=42.0        # AES-256-GCM, SHA-256
argon2-cffi>=23.1         # Argon2id KDF / password hashing
quantcrypt>=1.0.1         # PQC backend: ML-DSA / SLH-DSA / ML-KEM (prebuilt PQClean)

# --- Config ---
python-dotenv>=1.0
