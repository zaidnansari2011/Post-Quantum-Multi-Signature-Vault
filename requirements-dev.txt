# Development/test dependencies (in addition to requirements.txt).
# Install:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-flask>=1.3
pytest-cov>=5.0

# Formatters are PINNED, not floated. `black>=24.0` let CI install whatever was current on the day
# it happened to run, and black's style changes between majors -- so the same unmodified file was
# correctly formatted in one run and a failure in the next, through no commit of ours. By the time
# CI was pointed at this branch, 34 committed files disagreed with the installed formatter. A
# version range is the right thing for a library and the wrong thing for a tool whose entire job
# is to produce one canonical output. Raise this deliberately, in its own commit, and reformat in
# that same commit.
black==26.5.1

# Ruff moves fast and its default rule set grows; the same argument applies.
ruff==0.16.1

Faker>=25.0
